{
  "name": "secretfix-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "Stdio MCP server exposing secretfix's scan as a scan_repo tool, for AI agents (e.g. Venus) to call directly instead of only running on git commit.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.23.8"
  }
}
